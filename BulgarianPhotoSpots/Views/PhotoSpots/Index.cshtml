@{
    ViewData["Title"] = "Photo Spots";
}

<h1>Photo Spots in Bulgaria</h1>
<p>@ViewData["Message"]</p>

<ul>
    @foreach (var spot in (List<string>)ViewData["PhotoSpots"])
    {
        <li>@spot</li>
    }
</ul>

<a asp-action="Details" class="btn btn-secondary">
    View Details
</a>
